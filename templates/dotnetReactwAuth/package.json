{
	"name": "dotnetreact",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"start": "cd DotnetReact && cd Frontend && npm run cleanBuild && cd .. && dotnet run",
		"server": "nodemon --exec npm start",
		"preStart": "npm install && cd DotnetReact && dotnet restore && dotnet ef migrations add initial && dotnet ef database update && cd Frontend && npm install && cd .. && cd.."
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"nodemon": "^2.0.4",
		"rimraf": "^3.0.2"
	}
}
